services:
  notekeep:
    build: .
    container_name: notekeep
    ports:
      - "8696:8696"
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:///./data/notekeep.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
